const setup = () => {
    let change=document.querySelector("p").innerHTML="Good job!";
	// deze code wordt pas uitgevoerd als de pagina volledig is ingeladen
}

window.addEventListener("load", setup);